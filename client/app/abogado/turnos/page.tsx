import { BookingList } from "@/components";
const page = () => {
  return (
    <section className="relative min-h-screen bg-gray-400">
      <BookingList />
    </section>
  );
};

export default page;
