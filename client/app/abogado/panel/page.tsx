import { LawyerDashboard } from "@/components";

const page = () => {
  return (
    <section className="min-h-screen bg-gray-400">
      <LawyerDashboard />
    </section>
  );
};

export default page;
